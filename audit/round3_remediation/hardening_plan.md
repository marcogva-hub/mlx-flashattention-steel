# Volet K0 — Dispatch Grouping + Hardening Plan

Diagnostic only. Generated by `scripts/audit_dispatch_grouping_k0.py`; no product, validation, or kernel code is changed.

## Gather-barrier-absent siblings — first queue

- **None found among the 31 omitted entries.** Every source path that can gather/stage K/V in reusable shared memory either reaches the volet-J paged fix, the volet-S Sage fix, or an existing source-visible reuse barrier. Dense/NAX/MLX paths use direct device/pointer/tensor reads. [VERIFIED]

## Efficiency result

- **31 entries → 23 hardening groups** (1.35 entries/group). [VERIFIED]
- The public layer collapses to 7 groups; the raw layer requires 16 groups. Raw fragmentation is intentional here: entries with different current validation boundaries remain separate even when they share a primitive. [VERIFIED]

## Per-group plan

### 1. P1 — dense public adapters

- **Members (4):** `public:flash_attention_qkv_packed`, `public:flash_attention_kv_packed`, `public:flash_attention_speculative_verify`, `public:flash_attention_splitfuse` [VERIFIED]
- **Dispatch core:** packing/proxy logic → `flash_attention` → dense auto dispatch (SDPA, V6-NAX, STEEL/V2/V3, or flash-decode by shape/features). [VERIFIED]
- **Current validation:** Packed adapters validate layout/divisibility only; speculative verify does not validate `draft_ids` rank/batch/token count or temperature; splitfuse only rejects two absent Q branches. The downstream dense core validates ordinary Q/K/V contracts. [VERIFIED]
- **Shared-core hardening:** Keep dense Q/K/V validation centralized in `flash_attention`; add a small adapter-boundary validator for packed head counts/layouts and a splitfuse branch-triple validator before either dispatch.
- **Per-entry residual:** QKV/KV: positive head counts and 5-D GQA packing capacity. Speculative: `draft_ids` int dtype + `[B,N_draft]` exact shape, finite positive temperature. Splitfuse: each enabled Q/K/V triple complete and mutually compatible; reject cross-branch dtype/D disagreement if one scale is shared.
- **Gather/barrier:** ABSENT/N-A: no scattered-K/V gather. Dense auto uses Apple/direct-device paths on M5; legacy shared-memory dense paths already carry reuse barriers. [VERIFIED]
- **Source evidence:**

  - `mlx_mfa/attention.py:L8198` — packed adapters reach dense core [VERIFIED]
  - `mlx_mfa/attention.py:L4355` — speculative dense reaches dense core [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L196` — M5 dense pointer/direct-read path [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L836` — legacy shared-KV reuse barrier [VERIFIED]

### 2. P2 — RoPE-unified public family

- **Members (3):** `public:flash_attention_rope_unified`, `public:flash_attention_rope`, `public:flash_attention_kvcache_rope_append` [VERIFIED]
- **Dispatch core:** `flash_attention_rope_unified` → paged core, dense `flash_attention`, or raw `_mfa_rope_forward`/`mfa_attention_rope_forward`. [VERIFIED]
- **Current validation:** Unified boundary checks Q rank, table presence, per-batch offset count, and mode exclusions. Thin wrappers add no independent Q/K/V/cache checks. [VERIFIED]
- **Shared-core hardening:** Add one mode-aware validator in `flash_attention_rope_unified`: batch, K↔V seq/heads, GQA, mutual dtype/D, cache K↔V exact shape, and table dtype/shape/coverage before recursive per-batch dispatch.
- **Per-entry residual:** Standalone: K rotation range. Append: `k_cache`/`v_cache` paired and past-length agreement. Paged: pool/table/lens contract delegated once, but reject ignored K/V semantics explicitly. RoPE: even rotary width, `rotary_dim`, offset bounds, and cos/sin mutual shape/dtype.
- **Gather/barrier:** PRESENT where applicable: paged mode reaches the fixed paged gather; fused RoPE dense staging disables direct reads but has barrier X. No barrier-absent sibling. [VERIFIED]
- **Source evidence:**

  - `mlx_mfa/attention.py:L1503` — unified paged route [VERIFIED]
  - `mlx_mfa/attention.py:L1602` — fused RoPE route [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L196` — RoPE disables direct reads [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L836` — RoPE/shared-KV reuse barrier [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L3136` — paged gather reuse site [VERIFIED]

### 3. P3 — unified KV-cache router

- **Members (1):** `public:flash_attention_kvcache` [VERIFIED]
- **Dispatch core:** mode router → dense `flash_attention`, RoPE family, paged attention, and (paged append) raw `mfa_scatter_kv`. [VERIFIED]
- **Current validation:** Checks Q rank, mode exclusions/presence, several unsupported-feature combinations, equal heterogeneous RoPE offsets, and casts selected dtype mismatches. It does not establish a single complete cache/pool contract before mode-specific indexing/concatenation. [VERIFIED]
- **Shared-core hardening:** Add a mode discriminator followed by exactly one validator per dense, dense-append, paged, and paged-append mode. Each must cover batch, K↔V, GQA, mutual dtype/D, and metadata cardinality before indexing.
- **Per-entry residual:** Dense append: cache/new-token seq/head/D compatibility. Paged append: pool exact mutual shape/dtype, block capacity for every append position, seq_lens/table dtype/rank/cardinality, scatter index bounds. `cache_batch_idx`: int dtype/rank/bounds and output batch agreement.
- **Gather/barrier:** PRESENT: paged attention's scattered gather has the CX-J-02 start-of-loop barrier. `mfa_scatter_kv` is a copy/scatter primitive, not a K/V attention gather loop. Dense branches are pointer/direct or barriered dense staging. [VERIFIED]
- **Source evidence:**

  - `mlx_mfa/attention.py:L2703` — paged append scatter route [VERIFIED]
  - `mlx_mfa/attention.py:L2915` — paged consume route [VERIFIED]
  - `mlx_mfa/attention.py:L2954` — dense RoPE route [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L3136` — paged gather race site [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L3134` — paged barrier emitted [VERIFIED]

### 4. P4 — paged speculative adapter

- **Members (1):** `public:flash_attention_speculative_verify_paged` [VERIFIED]
- **Dispatch core:** proxy logic → `flash_attention_paged(return_lse=True)` → paged STEEL gather. [VERIFIED]
- **Current validation:** Checks `draft_ids` rank and batch only; paged core validates pool/table/lens. [VERIFIED]
- **Shared-core hardening:** Retain paged validation in the paged core; add exact speculative metadata validation before dispatch.
- **Per-entry residual:** `draft_ids` int dtype and `[B,N_q]` exact shape; finite positive temperature; reject impossible IDs rather than silently clipping if the API contract intends token IDs rather than D-axis proxy indices.
- **Gather/barrier:** PRESENT: scattered paged K/V gather has the CX-J-02 start-of-loop barrier. [VERIFIED]
- **Source evidence:**

  - `mlx_mfa/attention.py:L4410` — paged speculative route [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L3136` — paged gather source [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L3134` — paged barrier source [VERIFIED]

### 5. P5 — varlen packed adapters

- **Members (2):** `public:flash_attention_varlen_qkv_packed`, `public:flash_attention_varlen_kv_packed` [VERIFIED]
- **Dispatch core:** packing logic → `flash_attention_varlen` → raw `mfa_attention_varlen_forward` or per-sequence fallback. [VERIFIED]
- **Current validation:** Adapters validate packing layout/divisibility only. The public varlen core validates cumulative-sequence metadata and ordinary Q/K/V shape contracts. [VERIFIED]
- **Shared-core hardening:** Keep cumulative metadata/QKV checks in `flash_attention_varlen`; add packed layout/head-count checks before slicing so malformed 5-D GQA layouts cannot silently truncate heads.
- **Per-entry residual:** Positive `num_heads`/`num_kv_heads`; 5-D tensor head capacity for requested GQA; packed total-Q versus total-K semantics; explicit mutual packed dtype.
- **Gather/barrier:** PRESENT but not a scattered gather: packed-contiguous varlen stages K/V in shared memory and emits a start-of-iteration reuse barrier. [VERIFIED]
- **Source evidence:**

  - `mlx_mfa/attention.py:L8364` — packed varlen route [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L2305` — varlen kernel generator [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L2485` — varlen reuse barrier rationale [VERIFIED]

### 6. P6 — Sage KV-cache adapter

- **Members (1):** `public:sage_attention_kvcache` [VERIFIED]
- **Dispatch core:** `sage_attention` → `mfa_smooth_quantize_k`/quantize path → raw `mfa_sage_forward`. [VERIFIED]
- **Current validation:** The adapter adds no checks; `sage_attention` owns Q/K/V validation and preprocessing, while volet J hardened prequantized/raw buffers. [VERIFIED]
- **Shared-core hardening:** Keep one Sage Q/K/V validator in `sage_attention` and ensure the KV-cache alias cannot bypass it.
- **Per-entry residual:** Decode-specific contract: allow Nq≠Nkv but require K↔V seq/heads, GQA, batch, mutual dtype/D, non-empty KV, and supported window/causal semantics.
- **Gather/barrier:** PRESENT: Sage shared-KV gather has the volet-S inter-iteration barrier. [VERIFIED]
- **Source evidence:**

  - `mlx_mfa/attention.py:L2198` — Sage adapter route [VERIFIED]
  - `csrc/mfa_sage_fwd.cpp:L295` — Sage shared-memory barrier [VERIFIED]

### 7. P7 — top-k standalone

- **Members (1):** `public:flash_attention_topk` [VERIFIED]
- **Dispatch core:** MLX matmul/top-k or bisection threshold → Apple SDPA; otherwise MLX materialized-score reference. [VERIFIED]
- **Current validation:** Checks Q rank and ratio only. K/V are indexed and multiplied without a mutual contract check; optional mask is expanded without full dtype/shape validation. [VERIFIED]
- **Shared-core hardening:** Add a local top-k Q/K/V validator covering batch, K↔V, GQA-or-explicit-MHA policy, mutual dtype/D, and supported numeric dtypes.
- **Per-entry residual:** Mask bool dtype and exact tile shape; finite positive scale; finite inputs policy; define whether GQA is supported on both reference and SDPA branches.
- **Gather/barrier:** ABSENT/N-A: no custom scattered-K/V shared-memory gather. [VERIFIED]
- **Source evidence:**

  - `mlx_mfa/attention.py:L4249` — top-k NAX/SDPA route [VERIFIED]
  - `mlx_mfa/attention.py:L4280` — top-k reference route [VERIFIED]

### 8. R1 — raw dense MFA forward

- **Members (1):** `raw:mfa_attention_forward` [VERIFIED]
- **Dispatch core:** free function → `MFAttention` primitive → dense auto STEEL/ccv dispatch. [VERIFIED]
- **Current validation:** Checks rank, batch, K↔V seq/heads, and D membership. Missing GQA, q↔k/v D, and mutual/supported dtype checks. [VERIFIED]
- **Shared-core hardening:** Introduce a shared raw dense-QKV validator and call it before contiguous conversion.
- **Per-entry residual:** Preserve supported D/features; reject zero Hkv/sequence, invalid GQA, q/k/v D mismatch, and mixed/unsupported dtype.
- **Gather/barrier:** ABSENT/N-A: dense pointer/direct path on M5; legacy dense shared staging is barriered. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_attention.cpp:L1738` — raw core [VERIFIED]
  - `csrc/mfa_attention.cpp:L1806` — primitive dispatch [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L196` — M5 direct reads [VERIFIED]

### 9. R2 — raw dense MFA with LSE

- **Members (1):** `raw:mfa_forward_with_lse` [VERIFIED]
- **Dispatch core:** binding-local construction → `MFAttention` primitive. [VERIFIED]
- **Current validation:** Checks rank, batch, K↔V seq/heads, mutual D, GQA, D membership, and mutual dtype. [VERIFIED]
- **Shared-core hardening:** Route through the same shared raw dense-QKV validator as R1 to prevent drift.
- **Per-entry residual:** Add supported dtype and non-empty KV/head checks; preserve LSE shape/domain contract.
- **Gather/barrier:** ABSENT/N-A: same dense pointer/direct or barriered dense staging as R1. [VERIFIED]
- **Source evidence:**

  - `csrc/bindings.cpp:L221` — binding boundary [VERIFIED]
  - `csrc/bindings.cpp:L260` — primitive dispatch [VERIFIED]

### 10. R3 — raw ALiBi MFA

- **Members (1):** `raw:mfa_attention_alibi_forward` [VERIFIED]
- **Dispatch core:** free function → `MFAttention(has_alibi)` → dense STEEL. [VERIFIED]
- **Current validation:** Checks Q/K/V rank, ALiBi rank, and D membership; it does not validate Q/K/V mutual contract or slope count/dtype. [VERIFIED]
- **Shared-core hardening:** Call shared raw dense-QKV validator.
- **Per-entry residual:** Require slopes float32 (or explicitly cast), length Hq, finite values, and supported causal/GQA semantics.
- **Gather/barrier:** ABSENT/N-A: dense direct-device reads on M5; legacy dense staging barriered. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_attention.cpp:L1991` — ALiBi core [VERIFIED]
  - `csrc/mfa_attention.cpp:L2028` — ALiBi dispatch flag [VERIFIED]

### 11. R4 — raw bias MFA

- **Members (1):** `raw:mfa_attention_bias_forward` [VERIFIED]
- **Dispatch core:** free function → `MFAttention(has_attn_bias)` → dense STEEL. [VERIFIED]
- **Current validation:** Checks Q/K/V rank, bias mode/shape, D membership, and casts bias to float32; Q/K/V mutual contract remains unchecked. [VERIFIED]
- **Shared-core hardening:** Call shared raw dense-QKV validator.
- **Per-entry residual:** Bias finite-value policy; mode-2 head count already shape-locked but must remain after common-validator refactor.
- **Gather/barrier:** ABSENT/N-A: dense direct-device reads on M5; legacy dense staging barriered. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_attention.cpp:L2052` — bias core [VERIFIED]
  - `csrc/mfa_attention.cpp:L2114` — bias dispatch flag [VERIFIED]

### 12. R5 — raw fused-RoPE MFA

- **Members (1):** `raw:mfa_attention_rope_forward` [VERIFIED]
- **Dispatch core:** free function → `MFAttention(has_rope)` → STEEL shared-KV path. [VERIFIED]
- **Current validation:** Checks Q/K/V rank, D membership, and rejects Q float32 only. It does not validate Q/K/V mutual contract or rotary buffers. [VERIFIED]
- **Shared-core hardening:** Call shared raw dense-QKV validator before primitive construction.
- **Per-entry residual:** Cos/sin float32, exact mutual shape, width D/2, sufficient position rows for K and offset Q, non-negative offset, and interleaving constraints.
- **Gather/barrier:** PRESENT: RoPE disables M5 direct reads, reuses shared K/V, and emits barrier X. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_attention.cpp:L1927` — RoPE core [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L196` — RoPE forces shared path [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L836` — reuse barrier [VERIFIED]

### 13. R6 — raw MFA block-sparse pair

- **Members (2):** `raw:mfa_attention_sparse_forward`, `raw:mfa_attention_sparse_forward_with_lse` [VERIFIED]
- **Dispatch core:** free functions → `MFAttention(has_block_mask)` → STEEL sparse dispatch. [VERIFIED]
- **Current validation:** Both check Q/K/V rank, mask rank, D membership, and reject Q float32. They omit Q/K/V mutual contract plus mask dtype/tile/cardinality checks. [VERIFIED]
- **Shared-core hardening:** Add one shared raw sparse validator used by both functions.
- **Per-entry residual:** Batch/K↔V/GQA/mutual dtype+D; bool/uint8 policy; exact 2-D/3-D/4-D mask geometry against selected BQ/BK; LSE contract for the second entry.
- **Gather/barrier:** ABSENT/N-A for scattered gather: sparse tiles jump through contiguous K/V. Any shared-memory staging path is barriered. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_attention.cpp:L1816` — sparse core [VERIFIED]
  - `csrc/mfa_attention.cpp:L1867` — sparse LSE core [VERIFIED]
  - `csrc/mfa_steel_fwd_v2.cpp:L836` — shared staging barrier [VERIFIED]

### 14. R7 — raw packed-varlen MFA

- **Members (1):** `raw:mfa_attention_varlen_forward` [VERIFIED]
- **Dispatch core:** free function → `MFAVarlenAttention` → `SteelVarlenForward`. [VERIFIED]
- **Current validation:** No host shape validation; metadata are silently cast to int32 before dispatch. [VERIFIED]
- **Shared-core hardening:** Add a dedicated varlen raw validator before any `shape()` access or cast.
- **Per-entry residual:** Q/K/V rank `[1,H,total,D]`, batch=1, K↔V, GQA, mutual dtype/D, supported dtype/D; metadata int32 (no silent cast), rank/cardinality, monotonic starts at zero, terminal totals, tile-offset count/terminal value.
- **Gather/barrier:** PRESENT but not scattered: contiguous packed K/V shared staging has an inter-iteration barrier. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_attention.cpp:L2227` — varlen free function [VERIFIED]
  - `csrc/mfa_attention.cpp:L2253` — varlen primitive [VERIFIED]
  - `csrc/mfa_steel_fwd.cpp:L2485` — varlen barrier rationale [VERIFIED]

### 15. R8 — raw GNA

- **Members (1):** `raw:mfa_gna_forward` [VERIFIED]
- **Dispatch core:** free function → `MFAGNAForward` → GNA STEEL-V2 generator. [VERIFIED]
- **Current validation:** Checks rank, D=128, rejects Q float32, lattice product, GQA, batch, self-attention sequence equality, K↔V heads, and mutual D. Mixed dtype and invalid window/stride parameters remain unchecked. [VERIFIED]
- **Shared-core hardening:** Retain the existing shape validator and add mutual supported dtype checks.
- **Per-entry residual:** Positive dims/windows/strides, overflow-safe lattice product, window bounds semantics, and non-empty H/KV.
- **Gather/barrier:** ABSENT/N-A on M5: GNA sets direct device reads. Legacy shared staging has barriers on both sides of reuse. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_attention.cpp:L2931` — GNA free function [VERIFIED]
  - `csrc/mfa_gna_fwd.cpp:L46` — M5 direct reads [VERIFIED]
  - `csrc/mfa_gna_fwd.cpp:L272` — legacy shared barriers [VERIFIED]

### 16. R9 — raw NAX sparse forward

- **Members (1):** `raw:sparse_attention_forward` [VERIFIED]
- **Dispatch core:** free function → MLX `metal_kernel` V1 direct-device kernel or V2 NAX tile kernel. [VERIFIED]
- **Current validation:** Comprehensively checks rank, mutual dtype/shape, batch, GQA, D, block geometry, mask dtype/rank/shape, causal shape, scale, and mask address-space floor. [VERIFIED]
- **Shared-core hardening:** No shared-core validation change required; convert repeated checks into a helper only when R10 is hardened so the two variants cannot drift.
- **Per-entry residual:** Kernel-version enum should reject unknown explicit values rather than silently falling back; document/validate small-mask address-space contract.
- **Gather/barrier:** ABSENT/N-A: V1 reads device K/V rows directly; V2 uses device NAX tiles. No scattered shared-K/V gather. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_sparse_attention.cpp:L928` — NAX sparse core [VERIFIED]
  - `csrc/mfa_sparse_attention.cpp:L531` — V2 device pointer jump [VERIFIED]
  - `csrc/mfa_sparse_attention.cpp:L492` — V1 device reads [VERIFIED]

### 17. R10 — raw NAX sparse with LSE

- **Members (1):** `raw:sparse_attention_forward_with_lse` [VERIFIED]
- **Dispatch core:** free function → MLX `metal_kernel` V1 LSE kernel. [VERIFIED]
- **Current validation:** Duplicates only a subset of R9 validation: it lacks batch/K↔V/D mutual checks, full 3-D/4-D mask shape checks, causal qL=kL, and positive scale. [VERIFIED]
- **Shared-core hardening:** Extract and call the same sparse-input validator as R9, parameterized only for LSE capability/version.
- **Per-entry residual:** Preserve natural-log LSE and all-false `-INFINITY` sentinel contract.
- **Gather/barrier:** ABSENT/N-A: direct device K/V row reads; no scattered shared-K/V gather. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_sparse_attention.cpp:L1138` — NAX sparse LSE core [VERIFIED]
  - `csrc/mfa_sparse_attention.cpp:L1119` — LSE dispatch variant [VERIFIED]

### 18. R11 — raw per-block quantizer

- **Members (1):** `raw:mfa_quantize_per_block` [VERIFIED]
- **Dispatch core:** free function → `MFAQuantizePerBlock` primitive. [VERIFIED]
- **Current validation:** Checks rank, f16/bf16 dtype, and positive power-of-two block size. [VERIFIED]
- **Shared-core hardening:** No Q/K/V mutual validator applies; retain local validation.
- **Per-entry residual:** Reject empty dimensions if unsupported; bound block size and integer-derived grid sizes to avoid overflow/invalid threadgroup assumptions.
- **Gather/barrier:** ABSENT/N-A: quantization reduction, not attention K/V gathering. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_quantize.cpp:L47` — quantizer core [VERIFIED]
  - `csrc/mfa_quantize.cpp:L216` — current validator [VERIFIED]

### 19. R12 — raw smooth+quantize

- **Members (1):** `raw:mfa_smooth_quantize_k` [VERIFIED]
- **Dispatch core:** free function → `MFASmoothQuantizeK` two-pass primitive. [VERIFIED]
- **Current validation:** Checks rank, f16/bf16 dtype, and positive power-of-two block size. [VERIFIED]
- **Shared-core hardening:** Retain local validation; share scalar block-size helper with R11 only if the helper preserves each primitive's dimension constraints.
- **Per-entry residual:** Reject empty dimensions if unsupported; bound block size/grid arithmetic; lock scale-count and mean output shapes.
- **Gather/barrier:** ABSENT/N-A: preprocessing reduction/quantization, not attention K/V gathering. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_smooth_quant.cpp:L336` — smooth-quant core [VERIFIED]
  - `csrc/mfa_smooth_quant.cpp:L349` — current validator [VERIFIED]

### 20. R13 — raw paged scatter

- **Members (1):** `raw:mfa_scatter_kv` [VERIFIED]
- **Dispatch core:** free function → `MFAScatterKV` copy/scatter primitive. [VERIFIED]
- **Current validation:** Checks pool/tokens/index ranks, pool dtype, index dtype, token tail shape, and equal write counts. It does not require token dtype=pool or validate index bounds. [VERIFIED]
- **Shared-core hardening:** Keep local scatter validation and reject malformed writes before dispatch.
- **Per-entry residual:** Mutual pool/tokens dtype; every `blk_id` in `[0,num_blocks)` and `blk_off` in `[0,block_size)`; duplicate-target semantics; empty-write behavior.
- **Gather/barrier:** ABSENT/N-A: scatter copy has no iterative attention K/V shared-memory gather. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_scatter.cpp:L188` — scatter core [VERIFIED]
  - `csrc/mfa_scatter.cpp:L222` — current validator [VERIFIED]

### 21. R14 — raw Conv3D NAX

- **Members (1):** `raw:conv3d_nax_forward` [VERIFIED]
- **Dispatch core:** free function → eligible MPP convolution2d path or im2col/matmul fallback. [VERIFIED]
- **Current validation:** Checks ranks, mutual f16/bf16 dtype, C-in, stride/dilation/padding, and effective output size. [VERIFIED]
- **Shared-core hardening:** Retain Conv-specific validator; no attention shared core applies.
- **Per-entry residual:** Validate tuple arity at binding boundary, positive/non-empty dimensions, `chunk_M` domain, and overflow-safe M/K/N and byte-budget arithmetic.
- **Gather/barrier:** ABSENT/N-A: convolution/im2col path, not attention K/V gathering. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_conv_nax.cpp:L478` — Conv3D core [VERIFIED]
  - `csrc/mfa_conv_nax.cpp:L487` — current validator [VERIFIED]

### 22. R15 — raw V6-NAX forward

- **Members (1):** `raw:v6_nax_forward` [VERIFIED]
- **Dispatch core:** free function → `MFAV6Forward` → pure V6 NAX matmul2d kernel. [VERIFIED]
- **Current validation:** Checks Q rank and D only; it computes layout/GQA routing from K without validating K/V rank, batch, K↔V, GQA, dtype, or mutual D. [VERIFIED]
- **Shared-core hardening:** Add a V6 forward Q/K/V validator before reading K shape or transposing.
- **Per-entry residual:** Mutual supported f16/bf16 dtype, batch/K↔V/GQA/D, non-empty KV, causal support, finite positive/default scale semantics, and device capability.
- **Gather/barrier:** ABSENT/N-A: cooperative tensor/device tile reads; only P is materialized to shared memory, not K/V. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_v6_nax_primitive.cpp:L737` — V6 forward core [VERIFIED]
  - `csrc/mfa_steel_fwd_v6_nax.cpp:L134` — device/tensor K tile [VERIFIED]
  - `csrc/mfa_steel_fwd_v6_nax.cpp:L230` — only P shared staging [VERIFIED]

### 23. R16 — raw V6-NAX backward pair

- **Members (2):** `raw:v6_nax_backward_query`, `raw:v6_nax_backward_kv` [VERIFIED]
- **Dispatch core:** shared `v6_check_bwd_gqa` + aux-shape helpers → separate dQ and dK/dV primitives. [VERIFIED]
- **Current validation:** Both check Q/K/V rank, batch, K↔V seq/heads, GQA, mutual D, and exact O/LSE/dO/d_vec shapes. Dtype/domain checks are absent. [VERIFIED]
- **Shared-core hardening:** Extend the existing shared helpers with mutual supported dtype and LSE/d_vec float32 checks so both bindings harden together.
- **Per-entry residual:** D membership, non-empty KV, forward-eligibility/LSE convention, finite positive scale, causal support, and output dtype expectations.
- **Gather/barrier:** ABSENT/N-A: V6 backward uses cooperative/device tile reads, not scattered K/V shared-memory gathering. [VERIFIED]
- **Source evidence:**

  - `csrc/mfa_v6_nax_primitive.cpp:L37` — shared QKV validator [VERIFIED]
  - `csrc/mfa_v6_nax_primitive.cpp:L1220` — dQ core [VERIFIED]
  - `csrc/mfa_v6_nax_primitive.cpp:L1400` — dK/dV core [VERIFIED]

## 31-entry accounting ledger

| entry | group | dispatch core | gather classification |
|---|---|---|---|
| `public:flash_attention_qkv_packed` | P1 | packing/proxy logic → `flash_attention` → dense auto dispatch (SDPA, V6-NAX, STEEL/V2/V3, or flash-decode by shape/features) | ABSENT/N-A [VERIFIED] |
| `public:flash_attention_kv_packed` | P1 | packing/proxy logic → `flash_attention` → dense auto dispatch (SDPA, V6-NAX, STEEL/V2/V3, or flash-decode by shape/features) | ABSENT/N-A [VERIFIED] |
| `public:flash_attention_speculative_verify` | P1 | packing/proxy logic → `flash_attention` → dense auto dispatch (SDPA, V6-NAX, STEEL/V2/V3, or flash-decode by shape/features) | ABSENT/N-A [VERIFIED] |
| `public:flash_attention_splitfuse` | P1 | packing/proxy logic → `flash_attention` → dense auto dispatch (SDPA, V6-NAX, STEEL/V2/V3, or flash-decode by shape/features) | ABSENT/N-A [VERIFIED] |
| `public:flash_attention_rope_unified` | P2 | `flash_attention_rope_unified` → paged core, dense `flash_attention`, or raw `_mfa_rope_forward`/`mfa_attention_rope_forward` | PRESENT where applicable [VERIFIED] |
| `public:flash_attention_rope` | P2 | `flash_attention_rope_unified` → paged core, dense `flash_attention`, or raw `_mfa_rope_forward`/`mfa_attention_rope_forward` | PRESENT where applicable [VERIFIED] |
| `public:flash_attention_kvcache_rope_append` | P2 | `flash_attention_rope_unified` → paged core, dense `flash_attention`, or raw `_mfa_rope_forward`/`mfa_attention_rope_forward` | PRESENT where applicable [VERIFIED] |
| `public:flash_attention_kvcache` | P3 | mode router → dense `flash_attention`, RoPE family, paged attention, and (paged append) raw `mfa_scatter_kv` | PRESENT [VERIFIED] |
| `public:flash_attention_speculative_verify_paged` | P4 | proxy logic → `flash_attention_paged(return_lse=True)` → paged STEEL gather | PRESENT [VERIFIED] |
| `public:flash_attention_varlen_qkv_packed` | P5 | packing logic → `flash_attention_varlen` → raw `mfa_attention_varlen_forward` or per-sequence fallback | PRESENT but not a scattered gather [VERIFIED] |
| `public:flash_attention_varlen_kv_packed` | P5 | packing logic → `flash_attention_varlen` → raw `mfa_attention_varlen_forward` or per-sequence fallback | PRESENT but not a scattered gather [VERIFIED] |
| `public:sage_attention_kvcache` | P6 | `sage_attention` → `mfa_smooth_quantize_k`/quantize path → raw `mfa_sage_forward` | PRESENT [VERIFIED] |
| `public:flash_attention_topk` | P7 | MLX matmul/top-k or bisection threshold → Apple SDPA; otherwise MLX materialized-score reference | ABSENT/N-A [VERIFIED] |
| `raw:mfa_attention_forward` | R1 | free function → `MFAttention` primitive → dense auto STEEL/ccv dispatch | ABSENT/N-A [VERIFIED] |
| `raw:mfa_forward_with_lse` | R2 | binding-local construction → `MFAttention` primitive | ABSENT/N-A [VERIFIED] |
| `raw:mfa_attention_alibi_forward` | R3 | free function → `MFAttention(has_alibi)` → dense STEEL | ABSENT/N-A [VERIFIED] |
| `raw:mfa_attention_bias_forward` | R4 | free function → `MFAttention(has_attn_bias)` → dense STEEL | ABSENT/N-A [VERIFIED] |
| `raw:mfa_attention_rope_forward` | R5 | free function → `MFAttention(has_rope)` → STEEL shared-KV path | PRESENT [VERIFIED] |
| `raw:mfa_attention_sparse_forward` | R6 | free functions → `MFAttention(has_block_mask)` → STEEL sparse dispatch | ABSENT/N-A for scattered gather [VERIFIED] |
| `raw:mfa_attention_sparse_forward_with_lse` | R6 | free functions → `MFAttention(has_block_mask)` → STEEL sparse dispatch | ABSENT/N-A for scattered gather [VERIFIED] |
| `raw:mfa_attention_varlen_forward` | R7 | free function → `MFAVarlenAttention` → `SteelVarlenForward` | PRESENT but not scattered [VERIFIED] |
| `raw:mfa_gna_forward` | R8 | free function → `MFAGNAForward` → GNA STEEL-V2 generator | ABSENT/N-A on M5 [VERIFIED] |
| `raw:sparse_attention_forward` | R9 | free function → MLX `metal_kernel` V1 direct-device kernel or V2 NAX tile kernel | ABSENT/N-A [VERIFIED] |
| `raw:sparse_attention_forward_with_lse` | R10 | free function → MLX `metal_kernel` V1 LSE kernel | ABSENT/N-A [VERIFIED] |
| `raw:mfa_quantize_per_block` | R11 | free function → `MFAQuantizePerBlock` primitive | ABSENT/N-A [VERIFIED] |
| `raw:mfa_smooth_quantize_k` | R12 | free function → `MFASmoothQuantizeK` two-pass primitive | ABSENT/N-A [VERIFIED] |
| `raw:mfa_scatter_kv` | R13 | free function → `MFAScatterKV` copy/scatter primitive | ABSENT/N-A [VERIFIED] |
| `raw:conv3d_nax_forward` | R14 | free function → eligible MPP convolution2d path or im2col/matmul fallback | ABSENT/N-A [VERIFIED] |
| `raw:v6_nax_forward` | R15 | free function → `MFAV6Forward` → pure V6 NAX matmul2d kernel | ABSENT/N-A [VERIFIED] |
| `raw:v6_nax_backward_query` | R16 | shared `v6_check_bwd_gqa` + aux-shape helpers → separate dQ and dK/dV primitives | ABSENT/N-A [VERIFIED] |
| `raw:v6_nax_backward_kv` | R16 | shared `v6_check_bwd_gqa` + aux-shape helpers → separate dQ and dK/dV primitives | ABSENT/N-A [VERIFIED] |

Accounting: **31/31** canonical omitted entries assigned exactly once. [VERIFIED]

## K1 sizing / order

1. **Likely-CRITICAL queue:** R15 V6 forward, R7 raw varlen, R5 raw RoPE, R3/R4 feature raw MFA, R6 raw MFA sparse, R10 NAX sparse-LSE, R13 scatter. These boundaries currently read multiple buffers while missing one or more mutual shape/dtype/count checks. [DEDUCED from source-verified gaps]
2. **Shared validator wins:** R1–R5 share a dense-QKV base validator; R6 shares a sparse validator across its pair; R9/R10 should converge on one NAX-sparse validator; R16 already shares helpers. [DEDUCED]
3. **Public residual pass:** P1–P7 then add feature metadata checks after their downstream computational cores are hardened. [DEDUCED]
4. **Barrier action:** no new kernel barrier patch is prescribed by K0. Future K1 tests must still source-lock the paged, Sage, RoPE/shared, varlen, and legacy GNA barriers so validation work cannot regress determinism. [DEDUCED]

## Mechanical validation

Run:

```bash
.venv/bin/python scripts/audit_dispatch_grouping_k0.py --check
```

The check fails unless the volet-J omitted row set is exactly 31, every entry is assigned exactly once, required Python AST call edges exist, all 18 raw `m.def` bindings exist, and every gather/barrier claim's source needle is present.
