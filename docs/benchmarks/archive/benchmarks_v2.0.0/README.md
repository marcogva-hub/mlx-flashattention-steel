# Archived Benchmark JSON (Historical)

These JSON artifacts were moved out of active `docs/benchmarks/` during the
final freeze/prep cleanup pass.

Files:
- `backward_matrix.json`
- `dispatch_matrix.json`

Status:
- Historical reference only.
- Not part of the current benchmark status surface.
- Current benchmark interpretation is maintained in:
  `docs/benchmarks/RESULTS.md` and `devnotes/` track folders.
