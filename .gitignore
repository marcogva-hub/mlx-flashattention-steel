# Build
build/
dist/
*.egg-info/
*.eggs/
*.so
*.dylib
_ext*.pyd

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
# Deprecated venvs (post 2026-05-13 Sprint 1 consolidation; canonical is .venv/)
venv.deprecated.*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
._*

# Metal
*.metallib
!mlx_mfa/precompiled/async_v2.metallib
*.air
*.metal-ar

# Test / coverage artifacts
.pytest_cache/
htmlcov/
.coverage
*.coverage

# Environment
.env

# Jupyter
.ipynb_checkpoints/

# SSH keys / credentials (never commit)
*.pub
mfa-steel
mfa-steel.pub

# Debug / scratch artifacts
*_shader.txt
*_shader_dump.txt
shader_dump.txt
full_shader_dump.txt
*.ll
claude-code-prompt*.md
PROMPT_PHASE_*.md
TODO.md
_build_manual/
*.varlen_append

docs/v6-nax/captures/

# Sprint scratch artifacts (regenerated by bench scripts)
outputs/
docs/audit_dit_dispatch_output.log

# doc-release-prep skill artifacts (generated backups; restore from manifest if needed)
.doc-archive/
.archive/

# Local agent scratch — never ship (v2.58.1 P3: leaked into the 2.58.0 sdist)
.claude/

# AGENTS.md — Codex-side conventions, kept LOCAL (untracked).  Audit 2026-06-21
# H1/L7: it was untracked-and-not-gitignored, so scikit-build-core swept it into
# the sdist and reddened test_publish_surface_guard.  Gitignore stops the leak
# AND a stray `git add .`.  ALTERNATIVE (flagged for sign-off): track it + add to
# the publish allowlist so it ships like CLAUDE.md — pick one deliberately.
AGENTS.md

# Audit/report scratch deliverables — local only, never tracked or shipped.
AUDIT_REPORT_*.md

# Journal / probe-verdict scratch (archive-bound, never on the publish surface)
devnotes/

# CAMPAIGN.md — CC's internal campaign operating manual (methodology, not user-facing).
# Kept LOCAL (untracked) like devnotes/: reread each campaign task, never shipped in the sdist.
CAMPAIGN.md

# stray journal-export zip (cruft — real archive is .doc-archive/); keep out of the sdist surface
doc-archive.zip

# Campaign scratch / telemetry + devnotes exports — never shipped (publication guard)
debug/
devnotes_*.zip
